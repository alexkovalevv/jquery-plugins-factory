/**
 * Объявление глобальных переменных
 * @author Alex Kovalev <alex.kovalevv@gmail.com>
 * @copyright Alex Kovalev 01.11.2016
 * @version 1.0
 */


(function($) {
	'use strict';

	if( !$.aikaComponent.socialbuttons ) {
		$.aikaComponent.socialbuttons = {};
	}

	if( !$.aikaComponent.sbtns ) {
		$.aikaComponent.sbtns = {};
	}

	if( !$.aikaComponent.sbtns.buttons ) {
		$.aikaComponent.sbtns.buttons = {};
	}

	if( !$.aikaComponent.sbtns.themes ) {
		$.aikaComponent.sbtns.themes = {};
	}

	if( !$.aikaComponent.sbtns.control ) {
		$.aikaComponent.sbtns.control = {};
	}

})(jQuery);

