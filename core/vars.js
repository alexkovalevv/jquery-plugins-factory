/**
 * Varibles
 * @author Alex Kovalev <alex.kovalevv@gmail.com>
 * @copyright Alex Kovalev 01.11.2016
 * @version 1.0
 */

(function($) {
	'use strict';

	if( !$.aikaCore ) {
		$.aikaCore = {};
	}
	if( !$.aikaCore.base ) {
		$.aikaCore.base = {};
	}
	if( !$.aikaComponent ) {
		$.aikaComponent = {};
	}
	if( !$.aikaCore.i18n ) {
		$.aikaCore.i18n = {};
	}
	if( !$.aikaCore.tools ) {
		$.aikaCore.tools = {};
	}
	if( !$.aikaCore.storages ) {
		$.aikaCore.storages = {};
	}
	if( !$.aikaCore.filters ) {
		$.aikaCore.services = {};
	}
})(jQuery);