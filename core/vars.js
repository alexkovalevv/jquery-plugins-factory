/**
 * Varibles
 * @author Alex Kovalev <alex.kovalevv@gmail.com>
 * @copyright Alex Kovalev 01.11.2016
 * @version 1.0
 */

(function($) {
	'use strict';

	if( !$.aikaCore ) {
		$.aikaCore = {};
	}
	if( !$.aikaCore.plugin ) {
		$.aikaCore.plugin = {};
	}
	if( !$.aikaCore.base ) {
		$.aikaCore.base = {};
	}
	if( !$.aikaComponent ) {
		$.aikaComponent = {};
	}
	if( !$.aikaCore.lang ) {
		$.aikaCore.lang = {};
	}
	if( !$.aikaCore.tools ) {
		$.aikaCore.tools = {};
	}
	if( !$.aikaCore.storages ) {
		$.aikaCore.storages = {};
	}
	if( !$.aikaCore.filters ) {
		$.aikaCore.services = {};
	}
})(jQuery);